---
interface Props {
  text: string;
  tw?: string;
}

const { text, tw } = Astro.props;
---

<span
  class={`px-2 py-1 rounded-md text-cinnabar-500 bg-transparent border-2 border-cinnabar-500 text-base font-roboto select-none mr-4 h-min my-auto ${
    tw ?? ""
  }`}>{text}</span
>
